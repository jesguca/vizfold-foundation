# Contributing

Work should branch from this fork's `main`.

1. Pull the latest `main`.
2. Create a feature branch.
3. Make focused commits.
4. Open a pull request back into this fork's `main`.

Once changes are stable, they can be proposed upstream to `AI2Science/vizfold-foundation`.